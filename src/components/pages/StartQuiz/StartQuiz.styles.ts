import styled from "styled-components";
import { fontSize } from "../../../styles/global";
export const StartQuizStyles = styled.section`
  align-items: center;
  display: flex;
  flex-direction: column;
  justify-content: center;
  width: 100%;

  .title {
    font-size: ${fontSize.font30};
  }
`;
